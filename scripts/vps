#!/bin/sh
set -eu
cd "$(dirname "$0")/.."
if [ ! -f .env.vps ]; then
    echo 'Run python3 scripts/init-vps-env.py first.' >&2
    exit 1
fi
exec docker compose --env-file .env.vps -f compose.vps.yaml "$@"
